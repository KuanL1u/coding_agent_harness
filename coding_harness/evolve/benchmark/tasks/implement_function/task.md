Implement the `reverse_words` function in `strings.py`.

It takes a string and returns a new string with the whitespace-separated words
in reverse order, collapsing runs of whitespace to a single space. The existing
tests in `test_strings.py` specify the exact behaviour. Make them pass without
editing the tests, then call task_done.
